#!/usr/bin/python3
def best_score(a_dictionary):
    max_key, max_num = None, None
    for key, v in a_dictionary.items():
        if v > max:
            max_key = key
            max_num  v
    return max_key
